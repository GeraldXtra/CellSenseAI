// ChatWindow: the small floating chat window at the bottom right, built from ChatThread and ChatInput, with an x to close and an Open full page link. Owner: Osakue.
export default function ChatWindow() {}
